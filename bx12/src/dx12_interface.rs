// Interfaces and bindings for DX12 structures

pub struct Dx12Device {
    // Device internals
}

impl Dx12Device {
    pub fn new() -> Self {
        Dx12Device {
            // Init
        }
    }
}
